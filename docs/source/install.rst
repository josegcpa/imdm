Installation
============

The project should be readily instalable from `GitHub <https://github.com/josegcpa/imdm>`_
with ``poetry`` (recommended) by running ``poetry install``. It is also 
available in PyPi through ``pip install imdm``.
